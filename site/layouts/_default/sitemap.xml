{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Data.Pages }}
  {{ if .Permalink }}
  {{/* Per-section priority (Batch 9, 2026-07-02): homepage highest, section hubs next,
       city/country hubs above deep combinatorial pages; utility pages lowest. */}}
  {{ $priority := "0.5" }}
  {{ if .IsHome }}{{ $priority = "1.0" }}
  {{ else if eq .Kind "section" }}{{ $priority = "0.8" }}
  {{ else if in (slice "cities" "countries") .Section }}{{ $priority = "0.7" }}
  {{ else if in (slice "bodyguard-hire" "executive-protection" "close-protection-officers" "security-drivers" "residential-security" "secure-airport-transfers" "event-security" "blog" "guides" "risk-assessments" "services") .Section }}{{ $priority = "0.6" }}
  {{ else if in (slice "about" "contact" "faq" "privacy" "terms" "network") .File.ContentBaseName }}{{ $priority = "0.3" }}
  {{ end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{ if not .Lastmod.IsZero }}<lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}
    {{ with .Sitemap.ChangeFreq }}<changefreq>{{ . }}</changefreq>{{ end }}
    <priority>{{ $priority }}</priority>
  </url>
  {{ end }}
  {{ end }}
</urlset>
